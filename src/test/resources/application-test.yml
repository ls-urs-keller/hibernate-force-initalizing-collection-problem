spring:
  datasource:
    url: jdbc:mysql://hibernate_orm_test:hibernate_orm_test@localhost/hibernate_orm_test?allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
      hibernate:
        hbm2ddl:
          schema-generation:
            script:
              append: false
#      jakarta:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: create.sql
